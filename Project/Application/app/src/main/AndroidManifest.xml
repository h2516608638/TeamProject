<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animee.application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ping"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ping"
        android:supportsRtl="true"
        android:theme="@style/Theme.Application">
        <activity
            android:name=".food_grid.FoodDescActivity"
            android:exported="false" />
        <activity
            android:name=".food_grid.FoodGridActivity"
            android:exported="false" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".HomeMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".food_list.InfoListActivity" />
    </application>

</manifest>